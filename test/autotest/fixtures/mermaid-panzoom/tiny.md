# Tiny Mermaid Diagram

Smallest possible diagram — single node with an arrow. Used to verify the pan/zoom enhancer handles trivially small SVGs without stretching them inappropriately.

```mermaid
graph LR
    A --> B
```

End of tiny fixture.
